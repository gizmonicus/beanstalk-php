---
version: "2"
services:
  php:
    restart: never
    build:
      context: ./
    image: gizmonicus/beanstalk-php:latest
    volumes:
      - ./html:/var/www/html
    environment:
      - DEBUG=1
    network_mode: host
